#include "linkedlist.h"
#include <stdio.h>

void insert_front(int val, struct list* li)
{
}

void insert_after(int val, struct list* li)
{
}

void insert_middle(int val, int position, struct list* li)
{
}

void clear(struct list* li)
{
}

int size(struct list* li)
{
  return li->size;
}

struct node* search(int val, struct list* li)
{
  return 0;
}

int is_empty(struct list* li)
{
  return li->size == 0;
}

